// example of let 

// const callme = () =>{
//     if(true){
//         var let = 10;
//     }
//     console.log(val);
// }

// callme();

// let is a block scope and we can access inside the block if we try to access the let form outside the block then we get error 
// soo whenever we declear h let then we enure that are are access inside the block 

// example for the var 
// var is a function scope we can access var from side the function if we are try to access the var form outside teh function then we can't access the variable 

// (function(){
//     var c = 10;
//     console.log("This is the value of the var",c);
// })()

// console.log(c);

// if we are try to access the var form outside the function then we get the refernce error,
// so basically var is function scope that mean we can access var from inside the function and try access from outside the function then we get the error like var c is undefine;

